# Billboard Cultural Mood Analysis

AI-driven pipeline analyzing emotional, thematic, and psychological trends
in Billboard Hot 100 music from 1958 to the present.

## Quick Start
```bash
python3.11 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
pip install -e .
python -m main --dry-run
See full documentation in the project consolidation artifact. 
